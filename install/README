There is an (untested) fabfile.cfg that can be used with fabfile.py to ease 
installation of these scripts. You will still need to do some setup by hand,
including cronjobs. Install from this directory.

Requires python-fabric

